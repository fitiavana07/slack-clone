import React, { FC } from 'react'

const GroupTitle: FC = ({ children }) => (
  <p className="px-4 py-1 mb-1 text-sm font-medium uppercase text-gray-50">
    {children}
  </p>
)
export default GroupTitle
